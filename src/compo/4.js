/*
Now, let's enter the following command to install React 
Router into our project:
npm install react-router-dom

Let's also install the TypeScript types for React Router 
and save them as a development dependency:

npm install @types/react-router-dom --save-dev


-: Query parameters
A query parameter is part of the URL that allows additional 
parameters to be passed into a path. For example, 
"/products?search=redux" has a query parameter called 
search with a redux value.

  const [searchParams]: any = useSearchParams()
          // http://localhost:3000/pros?sort=name&order=ascending
          // console.log(searchParams.get('sort'));

          // for (const entry of searchParams.entries()) {
          //           console.log(entry);
          // }

cont on page 194
*/ 